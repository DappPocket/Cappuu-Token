{
  "name": "cappuu-token",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "npx oz compile",
    "start-chain": "ganache-cli --deterministic",
    "start-fork-chain": "ganache-cli --deterministic --fork https://mainnet.infura.io/v3/4c96c6bab18845dba07ad14cc0c18998",
    "deploy": "npx oz deploy",
    "test": "npx oz compile && npx mocha --exit --timeout 100000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/contracts": "^2.4.0",
    "@openzeppelin/test-environment": "^0.1.4",
    "@openzeppelin/test-helpers": "^0.5.6",
    "chai": "^4.2.0",
    "chai-bn": "^0.2.1",
    "ganache-cli": "^6.9.1",
    "mocha": "^7.1.2"
  }
}
